"use strict";
// function
const userData = (id, user) => {
    console.log(`Id #${id} and name is ${user.name} also age is ${user.age}`);
};
const sayHello = (id, user) => {
    console.log("asldkfa");
};
